export enum DeviceType {
  ELECTRIC = 'electric',
  WATER = 'water',
}

export enum DeviceStatus {
  ACTIVATED = 'activated',
  DEACTIVATE = 'deactivate',
}

export enum OrderBy {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum Action {
  Manage = 'manage',
  Create = 'create',
  Read = 'read',
  Update = 'update',
  Delete = 'delete',
}

export enum Role {
  User = 'ROLE_USER',
  Host = 'ROLE_HOST',
  Admin = 'ROLE_ADMIN',
}
