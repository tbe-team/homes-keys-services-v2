export enum DeviceType {
  ELECTRIC = 'electric',
  WATER = 'water',
}

export enum DeviceStatus {
  ACTIVATED = 'activated',
  DEACTIVATE = 'deactivate',
}

export enum OrderBy {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum Action {
  Manage = 'manage',
  Create = 'create',
  Read = 'read',
  Update = 'update',
  Delete = 'delete',
}
